meta {
  name: health
  type: http
  seq: 10
}

get {
  url: {{api_url}}/health
  body: none
  auth: inherit
}

assert {
  res.body.status: eq healthy
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
