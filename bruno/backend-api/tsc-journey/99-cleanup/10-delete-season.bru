meta {
  name: 10 Delete Season
  type: http
  seq: 10
}

delete {
  url: {{api_url}}/api/seasons/{{season_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("should delete season", function() {
    expect([200, 404]).to.include(res.status);
  });
}
