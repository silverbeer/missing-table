meta {
  name: 02 Delete Team 4
  type: http
  seq: 2
}

delete {
  url: {{api_url}}/api/teams/{{team_4_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("should delete team 4", function() {
    // 200 = deleted, 404 = already deleted
    expect([200, 404]).to.include(res.status);
  });
}
