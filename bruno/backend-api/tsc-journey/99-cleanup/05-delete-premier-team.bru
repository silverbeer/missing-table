meta {
  name: 05 Delete Premier Team
  type: http
  seq: 5
}

delete {
  url: {{api_url}}/api/teams/{{premier_team_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("should delete premier team", function() {
    expect([200, 404]).to.include(res.status);
  });
}
