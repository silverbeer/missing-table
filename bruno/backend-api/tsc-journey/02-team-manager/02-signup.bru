meta {
  name: 02 Signup Team Manager
  type: http
  seq: 2
}

post {
  url: {{api_url}}/api/auth/signup
  body: json
  auth: none
}

body:json {
  {
    "username": "{{team_manager_user}}",
    "password": "{{team_manager_pass}}",
    "display_name": "TSC Team Manager",
    "invite_code": "{{team_manager_invite_code}}"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("should signup team manager", function() {
    expect(res.status).to.equal(200);
  });
}
