# Railway Environment Variables for Missing Table
# Copy these to your Railway project settings

# === Backend Environment Variables ===
# Set these in your Railway backend service

# Supabase Configuration (from your Supabase project settings)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_JWT_SECRET=your-jwt-secret-from-supabase

# Application Configuration
ENVIRONMENT=production

# Security Keys (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
CSRF_SECRET_KEY=your-generated-csrf-secret-key

# Rate Limiting (Railway automatically provides REDIS_URL when you add Redis)
USE_REDIS_RATE_LIMIT=true
# REDIS_URL is automatically set by Railway when you add Redis service

# Railway automatically sets PORT variable

# === Frontend Environment Variables ===
# Set these in your Railway frontend service

# Supabase Configuration (same as backend)
VUE_APP_SUPABASE_URL=https://your-project-id.supabase.co
VUE_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Backend API URL (Railway backend domain)
VUE_APP_API_URL=https://your-backend-service.up.railway.app

# === Quick Setup Commands ===
# 1. Install Railway CLI: npm install -g @railway/cli
# 2. Login: railway login
# 3. Run setup script: ./deploy-railway.sh setup
# 4. Configure with your Supabase credentials: ./deploy-railway.sh configure
# 5. Deploy: ./deploy-railway.sh deploy