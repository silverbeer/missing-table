# Frontend Build Configuration
# Copy this file to build.env and fill in your values
# Usage: source build.env && docker buildx build ...

# API endpoint (environment-specific)
VUE_APP_API_URL=https://dev.missingtable.com

# Supabase configuration
VUE_APP_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (PUBLIC by design, protected by RLS)
# Get from: Supabase Dashboard > Settings > API > anon/public key
VUE_APP_SUPABASE_ANON_KEY=your-anon-key-here

# Security settings
VUE_APP_DISABLE_SECURITY=true
