apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: missing-table-ingress
  namespace: missing-table-prod
  annotations:
    # Use GCP Global Load Balancer
    kubernetes.io/ingress.class: "gce"
    # Enable HTTPS redirect
    kubernetes.io/ingress.allow-http: "true"
    # Use Google-managed SSL certificate
    networking.gke.io/managed-certificates: "missing-table-prod-cert"
    # Use the reserved static IP
    kubernetes.io/ingress.global-static-ip-name: "missing-table-prod-ip"
spec:
  rules:
  - host: missingtable.com
    http:
      paths:
      # Backend API routes (must come before catch-all)
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: missing-table-backend
            port:
              number: 8000
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: missing-table-backend
            port:
              number: 8000
      # Frontend (catch-all - must be last)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: missing-table-frontend
            port:
              number: 8080
  - host: www.missingtable.com
    http:
      paths:
      # Backend API routes (must come before catch-all)
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: missing-table-backend
            port:
              number: 8000
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: missing-table-backend
            port:
              number: 8000
      # Frontend (catch-all - must be last)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: missing-table-frontend
            port:
              number: 8080
---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: missing-table-prod-cert
  namespace: missing-table-prod
spec:
  domains:
    - missingtable.com
    - www.missingtable.com
